{
  "name": "payroll-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Collects weekly hours from workers via a shared link.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "start": "node --env-file-if-exists=.env src/index.js"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "express": "^4.21.2"
  }
}
